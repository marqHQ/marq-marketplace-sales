interface:
  display_name: "Upload New Skill"
  short_description: "Submit a new skill to the shared plugin"
  default_prompt: "Use $upload-new-skill to inspect and submit this new skill to the shared Marq Sales plugin for review."

dependencies:
  tools:
    - type: "mcp"
      value: "github"
      description: "GitHub MCP server"
      transport: "streamable_http"
      url: "https://api.githubcopilot.com/mcp/"
